#include "main.h"

/**
 *  _abs - computes abs value of an int
 *
 * @int: the int being printed
 *
 *  Return:the abs of an int
 */

int _abs(int)
{
	int n;

	if (n >= 0)
		return (n);
	else
		return (-n);
}
