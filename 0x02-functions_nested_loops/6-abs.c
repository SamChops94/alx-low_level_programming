#include "main.h"

/**
 *  _abs - computes abs value of an int
 *
 * @n: the int being printed
 *
 *  Return:the abs of an int
 */

int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (-n);
}
